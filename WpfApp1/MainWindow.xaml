
<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        xmlns:uc="clr-namespace:WpfApp1"
        xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
        Title="MainWindow" Height="450" Width="800">

    
    <Window.DataContext>
        <local:TestDataContext/>
    </Window.DataContext>
    <Window.Resources>
        <local:ImageValueConverter x:Key="ImageValueConverter"/>
        <DataTemplate  x:Key="CatalogItemDragAdorner">
            <StackPanel >
                <Image  Source="{ Binding Path=XamlMarkup, Converter={StaticResource ImageValueConverter}}" />
                <TextBlock Text="{Binding OrderCode}" HorizontalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    
    <StackPanel>


        <uc:Terminal HorizontalContentAlignment="Stretch" 
                     Modules="{Binding Devices, Mode=TwoWay}" 
                     SelectedModule="{Binding SelectedDevice, Mode=TwoWay}"
                     PasteCommand="{Binding PasteDevice, Mode=TwoWay}"
                     DoubleClickCommand="{Binding DoubleClickDevice, Mode=TwoWay}"/>
        
        <Button Content="Add device" Command="{Binding AddDevice}"/>
        

        
        <!--styling module catalogue items-->


            <ListView
                Grid.Row="4"
            ItemsSource="{Binding CatalogItems}"
            
                          SelectionMode="Single"
                          dd:DragDrop.IsDragSource="True"
                        dd:DragDrop.DragAdornerTemplate="{DynamicResource CatalogItemDragAdorner}">


                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>

                            <TextBlock FontWeight="Bold"
                   Grid.Row="0"
                   Grid.Column="0"
                   Text="{Binding OrderCode}"
                                       
                   TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" ToolTip="{Binding Tooltip}"/>

                        
                        <TextBlock TextAlignment="Right"
                   HorizontalAlignment="Right"
                   Grid.Row="0"
                   Grid.Column="1"
                   VerticalAlignment="Top"
                   Text="8129111"
                   TextTrimming="None" TextWrapping="NoWrap"
                   Margin="0,0,8,0" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>


            </ListView>
        
        
        
    </StackPanel>
</Window>
