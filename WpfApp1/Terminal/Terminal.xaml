<UserControl x:Class="WpfApp1.Terminal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:WpfApp1"
             mc:Ignorable="d" 
             x:Name="uc"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="ListBoxDragDrop" TargetType="{x:Type ListBoxItem}">
            <Setter Property="AllowDrop" Value="true"/>
            <EventSetter Event="PreviewMouseMove" Handler="listboxItem_PreviewMouseMoveEvent"/>
            <EventSetter Event="Drop" Handler="listboxItem_Drop"/>
            <EventSetter Event="GiveFeedback" Handler="listboxItem_GiveFeedback"/>
            <EventSetter Event="MouseDoubleClick" Handler="listBoxItem_MouseDoubleClick" />
        </Style>
    </UserControl.Resources>

    <Grid DataContext="{Binding RelativeSource={RelativeSource FindAncestor, 
                               AncestorType={x:Type local:Terminal}}}">
        <ListBox x:Name="listbox" 
                 Background="Azure"
                 HorizontalContentAlignment="Center"
                 ItemContainerStyle="{StaticResource ListBoxDragDrop}"
                 
                 
                  ItemsSource="{Binding RelativeSource={RelativeSource FindAncestor, 
                               AncestorType={x:Type local:Terminal}},
                               Path=Modules}"
                 SelectionMode="Single"
                 
                 
                 PreviewKeyDown="listbox_PreviewKeyDown"              
                 SelectionChanged="listbox_SelectionChanged">
            
            <ListBox.ContextMenu>
                <ContextMenu Name="ctxMenu">
                    <MenuItem Header="Delete" IsEnabled="{Binding IsItemSelected }" Click="deleteModule"/>
                    <MenuItem Header="Shift right" IsEnabled="{Binding IsItemSelected }" Click="shiftRight"/>
                    <MenuItem Header="Shift left"  IsEnabled="{Binding IsItemSelected }" Click="shiftLeft"/>

                </ContextMenu>
            </ListBox.ContextMenu>


            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Viewbox
                        
                        Stretch="Uniform"
						VerticalAlignment="Stretch"
						HorizontalAlignment="Stretch">
                        <ContentPresenter 
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
							Content="{Binding DeviceImage}"/>
                    </Viewbox>
                </DataTemplate>
                

            </ListBox.ItemTemplate>
        </ListBox>
        
    </Grid>
</UserControl>
